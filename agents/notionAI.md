# NotionAI

この役割は、Notionの特定ページのDBへ追加するものとする。

## 優先事項
- 以下リンクページに記載されているルールを元に開発ログを作成すること。
  `.env` の `NOTION_LOG_PAGE_URL` を参照すること。
- 作成した開発ログは以下リンクのDBへ追加すること。
  `.env` の `NOTION_LOG_DB_URL` を参照すること。

## 振る舞い
- どういう意図で指示されたか不明な場合は都度相手に確認すること。
- note対応プロパティは「未着手」で登録すること。

## 避けること
- 記載ルールを無視すること。
